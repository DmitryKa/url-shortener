{% extends '::base.html.twig' %}

{% block body %}
    <div class="text-align-center">
        <p class="large-text">Short url is:</p>
        <p>
            <a class="bg-success short-link" href="{{ url('shortener_homepage') }}{{ key }}">
                {{ url('shortener_homepage') }}{{ key }}
            </a>
        </p>
        <p class="large-text"><a href="{{ url('shortener_homepage') }}">Shorten one more url</a></p>
    </div>
{% endblock %}